import Button from "../common/Button/Button";

export default function AddRouletteButton() {
  return (
    <Button type={"button"} onClick={() => {}} color={"black"} size={"small"}>
      추가하기
    </Button>
  );
}
